/**
 * viewportSync.js - DISABLED VERSION
 * Viewport synchronization disabled due to PixiJS compatibility issues
 */

// Stub functions to maintain compatibility
export function syncToPixi() { return; }
export function syncResize() { return; }